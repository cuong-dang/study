\documentclass{article}
\usepackage[margin=1in]{geometry}
\usepackage{amsmath, amsthm, booktabs, enumitem, multicol, amssymb}

\everymath{\displaystyle}
\newtheorem*{definition*}{Definition}
\newtheorem*{proposition*}{Proposition}
\newtheorem*{theorem*}{Theorem}

\title{James Stewart Calculus}
\author{}
\date{}

\begin{document}
\maketitle
\setcounter{section}{2}
\section{Applications of Differentiation}
\subsection{Maximum and Minimum Values}
\begin{definition*}
  Let $c$ be a number in the domain $D$ of a function $f$. Then $f(c)$ is the
  \begin{itemize}
    \item \textbf{absolute maximum} value of $f$ on $D$ if $f(c)\ge f(x)$ for all $x$ in $D$.
    \item \textbf{absolte minimum} value of $f$ on $D$ if $f(c)\le f(x)$ for all $x$ in $D$.
  \end{itemize}
\end{definition*}
\begin{theorem*}[The Extreme Value Theorem]
  If $f$ is continuous on a closed interval $[a, b]$, then $f$ attains an absolute maximum value
  $f(c)$ and an absolute minimum value $f(d)$ at some numbers $c$ and $d$ in $[a, b]$.
\end{theorem*}
\begin{theorem*}[Fermat's Theorem]
  If $f$ has a local maximum or minimum at $c$, and if $f'(c)$ exists, then $f'(c) = 0$.
\end{theorem*}
\begin{proof}
  Suppose, for the sake of definiteness, that $f$ has a local maximum at $c$. Then, according to
  Definition 2, $f(c)\ge f(x)$ if $x$ is sufficiently close to $c$. This implies that if $h$ is
  sufficiently close to 0, with $h$ being positive or negative, then
  \[f(c)\ge f(c+h)\]
  and therefore
  \[f(c+h)-f(c)\le0.\]
  We can divide both sides of an inequality by a positive number. Thus, if $h>0$ and $h$ is
  sufficiently small, we have
  \[\frac{f(c+h)-f(c)}{h}\le0.\]
  Taking the right-hand limit of both sides of this inequality (using Theorem 1.6.2), we get
  \[\lim_{h\to0^+}\frac{f(c+h)-f(c)}{h}\le\lim_{h\to0^+}0=0.\]
  $\ldots$
\end{proof}
\begin{definition*}
  A \textbf{critical number} of a function $f$ is a number $c$ in the domain of $f$ such that either
  $f'(c)=0$ or $f'(c)$ does not exist.
\end{definition*}
\subsubsection*{Exercises}
\begin{enumerate}
    \setcounter{enumi}{56}
  \item $f(\frac{a}{a+b})$
\end{enumerate}

\subsection{The Mean Value Theorem}
\begin{theorem*}[The Mean Value Theorem]
  Let $f$ be a function that satisfies the following hypotheses:
  \begin{enumerate}
    \item $f$ is continuous on the closed interval $[a, b]$.
    \item $f$ is differentiable on the open interval $(a, b)$.
  \end{enumerate}
  Then there is a number $c$ in $(a, b)$ such that
  \[f'(c)=\frac{f(b)-f(a)}{b-a}\]
  or, equivalently,
  \[f(b)-f(a)=f'(c)(b-a).\]
\end{theorem*}
\subsubsection*{Exercises}
\begin{enumerate}
    \setcounter{enumi}{8}
  \item Not differentiable at 0.
    \setcounter{enumi}{16}
  \item Not continuous on the interval.
  \item Not differentiable on the interval.
    \setcounter{enumi}{24}
  \item 16
    \setcounter{enumi}{26}
  \item No
\end{enumerate}

\subsection{How Derivatives Affect the Shape of a Graph}
\begin{enumerate}
    \setcounter{enumi}{6}
  \item
    \begin{enumerate}
      \item 3, 5
      \item 2, 4, 6
      \item 1, 7
    \end{enumerate}
    \setcounter{enumi}{58}
  \item $\frac{2}{9}x^{3}+\frac{1}{3}x^{2}-\frac{4}{3}x+\frac{7}{9}$
    \setcounter{enumi}{56}
  \item $f(x)$ is increasing.
    \setcounter{enumi}{70}
  \item Consider function $g=f'$. Because $f''(c)$ is an inflection point, this means that $f''(c)$
    changes sign from negative to positive or from positive to negative at $c$. Thus, $f''(c)=g'(c)$
    changes sign at $c$. Applything the first derivative test gives us that $g$ has a local minimum
    or local maximum at $c$. Applying Fermant's Theorem, because $g'(c)$, which equals $f''(c)$
    exists, $g'(c)=f''(c)=0$.
    \setcounter{enumi}{74}
  \item Let $a,b\in I$ with $a<b$. We want to show $f(a)<f(b)$. Since $f$ is differentiable on $I$,
    it is continuous on $I$. We are given that $f'(x)>0$ for all $x\in I, x\ne c,$ where $c$ is a
    single exceptional point. We consider two cases.

    \textbf{Case 1:} $c\notin(a, b)$

    Then $f'(x)>0$ for every $x\in(a, b)$. By the Mean Value Theorem, there exists some $k\in(a,b)$
    such that $f(b)-f(a)=f'(k)(b-a)$. Since $b-a>0$ and $f'(k)>0$, $f(b)-f(a)>0$. Hence,
    $f(a)<f(b)$.

    \textbf{Case 2:} $c\in(a, b)$

    Apply the Mean Value Theorem separately on $[a, c]$ and $[c, b]$. There exist points $k_1\in(a,
    c), k_2\in(c, b)$, such that $f(c)-f(a)=f'(k_1)(c-1)$ and $f(b)-f(c)=f'(k_2)(b-c)$. Because $k_1
    \ne c$ and $k_2\ne c$, we have $f'(k_1)>0, f'(k_2)>0$. Also, $c-a>0, b-c>0$. Therefore, $f(c)-
    f(a)>0, f(b)-f(c)>0$. Adding these qualities gives $f(b)-f(a)>0$. So again, $f(a)<f(b)$.

    Since for every $a<b$ in $I$, we have $f(a)<f(b)$, the function $f$ is increasing on $I$.
  \item $c\ge\frac{1}{8}$
\end{enumerate}

\subsection{Limits at Infinity; Horizontal Asymptotes}
\begin{enumerate}
    \setcounter{enumi}{2}
  \item
    \begin{enumerate}
      \item -2
      \item 2
      \item $\infty$
      \item $-\infty$
    \end{enumerate}
    \setcounter{enumi}{6}
  \item $\frac{2}{5}$
  \item 3
  \item $\frac{3}{2}$
  \item 0
  \item 0
  \item 2
  \item $-1$
    \setcounter{enumi}{32}
  \item
    \begin{enumerate}
      \item $-\frac{1}{2}$
    \end{enumerate}
    \setcounter{enumi}{44}
  \item $\frac{x-2}{-x^2(x-3)}$
    \setcounter{enumi}{46}
  \item
    \begin{enumerate}
      \item $\frac{5}{4}$
      \item 5
    \end{enumerate}
    \setcounter{enumi}{60}
  \item
    \begin{enumerate}
      \item 0
      \item $\lfloor\frac{x}{\pi}\rfloor$ times
    \end{enumerate}
    \setcounter{enumi}{72}
  \item
    \begin{enumerate}
      \item Let $L=\lim_{x\to\infty}f(x)$. By definition, for every $\varepsilon>0$, there exists
        $M>0$ such that if $x>M$, then $|f(x)-L|<\varepsilon$.

        Let $t=1/x$. Then $x>M$ is equivalent to $0<t<\frac{1}{M}$. Then we can say for every
        $\varepsilon>0$, there exists $\delta=\frac{1}{M}$ such that if $0<t<\delta$, then $|f(x)-L|
        =|f(1/t)-L|<\varepsilon$. Then, by the definition of limit, $\lim_{t\to0^+}f(1/t)=L$.
      \item 0
    \end{enumerate}
\end{enumerate}

\subsection{Summary of Curve Sketching}
\subsection{Graphing with Calculus \textit{and} Calculators}
\subsection{Optimization Problems}
\subsection{Newton's Method}
\begin{enumerate}
    \setcounter{enumi}{2}
  \item 4.5
    \setcounter{enumi}{6}
  \item 1.5215
    \setcounter{enumi}{8}
  \item $-\frac{5}{4}$
    \setcounter{enumi}{10}
  \item 2.94283096
    \setcounter{enumi}{14}
  \item 0.876726
    \setcounter{enumi}{35}
  \item (1.519855, 2.306964)
    \setcounter{enumi}{36}
  \item (0.410245, 0.347810)
    \setcounter{enumi}{38}
  \item 0.78286\%
\end{enumerate}

\subsection{Antiderivatives}
\begin{enumerate}
  \item $2x^2+7x+C$
    \setcounter{enumi}{4}
  \item $4x^3+4x^2+C$
    \setcounter{enumi}{14}
  \item $2t^\frac{1}{2}+\frac{2}{3}t^\frac{3}{2}+\frac{2}{5}t^\frac{5}{2}+C$
    \setcounter{enumi}{16}
  \item $-2\cos\theta-\tan\theta+C$
    \setcounter{enumi}{20}
  \item $x^5-\frac{1}{3}x^6+4$
    \setcounter{enumi}{42}
  \item 4
    \setcounter{enumi}{44}
  \item $b$
    \setcounter{enumi}{58}
  \item
    \begin{enumerate}
      \item $-4.9t^2+450$
      \item $\approx9.58$ sec
      \item $-42\sqrt{5}$ m/s
      \item $\approx9.09$ sec
    \end{enumerate}
    \setcounter{enumi}{68}
  \item 5.87 ft/s$^2$
    \setcounter{enumi}{72}
  \item
    \begin{enumerate}
      \item $\approx22.9125$ mi
      \item $\approx21.675$ mi
    \end{enumerate}
\end{enumerate}

\subsection*{Review}
\subsubsection*{Concept Check}
\subsubsection*{True-False Quiz}
\begin{enumerate}
  \item False
  \item False
  \item False
  \item True
  \item True
  \item False
  \item False
  \item True
  \item True
  \item False
  \item True
  \item False
  \item False
  \item True
  \item True
  \item False
  \item True
  \item True
  \item True
\end{enumerate}
\end{document}
